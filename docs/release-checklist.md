# 潮汐灯塔：独立发布检查清单

基线：2026-09-05 产品 [规则契约](rule-contract.md)、[来源记录](provenance.md)、`themed-logic-game-studio` 的规则真值与发布要求，以及已读取的官方 **minitool-zip-builder 1.6.0**（ZIP、端能力、JS、CSS、跨端、性能 references）。本文件是待执行检查清单，不是测试报告；只有有对应证据时才勾选。

## 来源与交付范围

- [x] 官方 Net 手册实际读取；ebnbin 固定提交、规则、参考 C/TS 与两份许可逐一核对，版本和指纹记于 provenance。
- [x] 明确引擎衍生自已有 Storm Lanterns JavaScript，实现许可完整保留；没有把上游研究写成上游代码移植。
- [x] 实际图片与其他外部素材有文件级来源；最终页面许可内容与根 LICENSE / 第三方声明一致。
- [x] 根目录只保留本独立游戏，存档命名空间不读写原合集或星际漂流；原项目未因本次构建/发布被改动。
- [x] 每日与自由航行明确为额外离线验证的有限题库轮换；宣传不写无限生成、不厌倦保证、未经证明的最优步数。

## 引擎、关卡与持久化

- [x] `npm test` 全部通过，记录实际测试数量、命令、代码版本；不能用源码正则检查替代行为测试。
- [x] 48 个战役关、6 章每章 8 关；教程与每日/自由题库另行核对，题目 ID 与来源稳定。
- [x] 最大棋盘 6×6；题面均非空端口、初始未完成且旋转保持形状；教程三图来自真实 3×3 题面和合法操作。
- [x] 每道发布题由不读取预存解的独立求解器搜索至第二解上限 2；唯一性记录区分完整结束、发现第二解与预算耗尽。
- [x] 所有题解经生产引擎回放，显式验证接口相互匹配、边缘封闭、全部可达与无环；对称模块的同一朝向不得重复计为不同解。
- [x] 非法、锁定与同状态操作为原子 no-op；顺/逆旋转、撤销、重做、重开、提示和重复通关行为各有测试。
- [x] 锁定只是笔记；辅助高亮/音效/动画/奖励不能制造通关；切题或重开后旧动画和结算回调无效。
- [x] 存档校验已知题目、形状、动作、历史与胜利真值；仅伪造完成布尔不能创建奖励，恢复须经规则校验，刷新恢复和重复完成幂等。
- [x] localStorage 异常会诚实提示；不使用 `localStorage.clear()`；不宣称永久保存或云同步。
- [x] 生成与证明只在发布前执行；最终入口不会在启动、切题或提示时运行生成/穷举搜索。

## 最终构建与小红书静态包

- [x] `npm run build` 成功。读取实际 ZIP，不仅检查源目录；记录 ZIP 路径、字节数、SHA-256、包内清单及最终源代码提交。
- [x] ZIP 根目录直接是唯一 `index.html`，没有额外外层目录、绝对路径或 `..`；与最终小红书专用构建目录每个文件逐字节匹配（网页版入口与平台占位不同）。
- [x] 只含 `.html/.css/.js/.png/.jpg/.jpeg/.gif/.webp/.svg/.woff/.woff2/.json`；无 `.mjs/.md`、无扩展名 LICENSE、测试、构建配置、source map、node_modules、Git、机器路径或会话记录。
- [x] HTML 的 doctype、中文 lang、UTF-8、viewport 的设备宽度/初始比例/viewport-fit 齐全；引用资源全部包内相对路径且存在。
- [x] 经典外置脚本按依赖顺序加载；无内联脚本、行内事件、module/import/export、外部脚本、动态执行或 WASM。
- [x] 使用目标 ES2017 的解析器检查最终每个 `.js`，不是仅用当前 Node 解析；现代运行时 API 仅有实际需要的局部回退。
- [x] 按官方端能力清单检查最终 JS/HTML/CSS：无联网、Worker/ServiceWorker、硬件/传感器、剪贴板、站外导航、下载、iframe/object、自建 CSP、禁止的窗口/屏幕 API。
- [x] 本产品不需要 Native 桥接或权限；若以后增加，先另读官方 JSBridge reference 并按真实授权重审。
- [x] 许可完整可见且无需联网；许可文本由根第三方声明直接编入经典脚本，关于视图不依赖 fetch 加载。
- [x] 用官方 `audit_artifact.py` 或对应 Node 脚本分别审目录与最终 ZIP；记录原始结果与警告处理。此脚本只检查确定性体积门槛，不代表 API、语义或布局通过。
- [x] ZIP ≤ 10 MiB，目标 ≤ 2 MiB；单条 Base64 解码 ≤ 1 MiB，超过 100 KiB 优先外置；单文本超过 2 MiB 或文本总量超过 5 MiB 另作人工复核。

## CSS、触摸、弹层与性能

- [x] 最终 CSS 为 Chrome 61 基线：Grid 用 grid-gap；Flex 间距使用 margin 或真实行为检测；现代尺寸函数、逻辑属性、动态视口、aspect-ratio、现代选择器均不能是唯一关键实现。
- [x] 安全区有普通值和 `var(--safe-area-inset-*,0px)` 基线；`env()` 增强受合适的能力条件保护；同时考虑宿主导航占位，不把真实安全区等同于导航高度。
- [ ] 实际验证 320×450、375×450 半屏，320×720、390×844 手机，横屏和桌面；棋盘、菜单、图例、工具区、进度与弹层无重叠/裁切/横向溢出。
- [ ] 320 px 下核心格子及主按钮至少 44 px；图标固定可读尺寸、文字有独立间距。不能靠缩放页面或移动端横滑完成核心操作。
- [ ] 顺/逆旋转与锁定可用触摸和键盘完成；滚动不误操作棋盘，输入不依赖 hover，焦点可见。
- [ ] 首次教程、跳过、重看、真实合法旋转、提示、撤销/重做、通关、重复通关、刷新恢复、每日与自由切换均实际操作并记录。
- [ ] 弹层可关闭、内部可滚动，背景滚动受控；焦点顺序合理，无原生 dialog 的回退可用；快速切换与页面隐藏/恢复不会遗留锁死。
- [ ] 减少动态效果设置保留状态区分；音效只在用户操作后启动，静音/音频不可用不阻断游戏；隐藏页面停止不必要动画。
- [ ] 静态资源和题库适度，首屏不做生成/搜索，不每帧大量重建 DOM；性能结论有设备、方法和实测结果支持。
- [ ] Chrome 61 / Android 8.1、iOS 和真实移动性能分别记录；未实测时明确写未实测，现代桌面模拟通过不能替代。

## 三个平台发布验收

- [ ] 发布 GitHub 独立仓库，确认实际提交、仓库 URL、README、许可证与源代码内容；不以本地 commit 代替远端 push 成功。
- [ ] Vercel 项目绑定正确独立仓库；等待本次部署 READY，并核对部署对应的提交 SHA、生产域名、入口与关键资源内容/哈希。
- [ ] 在小红书实际上传最终相同指纹的 ZIP，完成教程、一次操作、撤销、通关、模式恢复与关于页预览；上传成功与平台审核/公开发布分别记录。
- [ ] 小红书宿主返回 → 再从卡片进入路径实际复验；iframe 未加载或停在空页时不能记为应用通过。导航遮挡与短视口布局用最终包截图确认。
- [ ] 平台如提出新的协议、权利保证或其他需用户确认事项，由主线程按实际授权处理；审核代理不代签，也不因普通构建/上传重复请求授权。
- [ ] 最终记录仓库与线上 URL、提交、部署状态、ZIP 指纹、测试/预览证据、平台审核状态和未测边界。最终产物变化后重算指纹并重跑受影响检查。

## 本次已执行证据

- 2026-09-05：214 项测试通过（183 规则与全库 + 31 独立存储/生产入口行为），无失败或跳过。
- 正式构建成功；最终小红书 ZIP 288,261 字节，SHA-256 `a2a9e5c00b4ea103233ebb5006c4298c56fd311b503cab4564ae20aeefa31958`，12 文件逐字节对应小红书构建目录。
- 官方目录审计与最终 ZIP 审计均为 `PASS: 12 file(s), 0 warning(s)`；该脚本不代表运行时或视觉验证。
- Vercel 独立项目生产部署 READY，匿名全部资源 12/12 一致；GitHub 公开仓库已创建并分目录提交源码，Vercel CLI 已返回 Git Connected；最终远端独立构建与内容核对另行记录，详见 deployment.md。
- 解锁后已实际走查原生 Chrome 桌面教程/旋转/锁定/撤销/重做/首胜/刷新恢复，以及 320×450、375×450、375×812 iframe。最新 320×450 六列棋盘与全部工具可完整访问；图解、方向提示、每日、自由、收藏及长许可弹窗已操作。未把这些检查扩展为全部设备矩阵通过。
- 小红书最终 ZIP 已部署成功；官方模拟器教程、旋转、撤销/重做、两步通关已通过。宿主返回后再次进入出现空页/页面不见了，该路线未通过；重新上传同指纹包恢复初次预览。待明确协议确认后提交审核。
- Chrome 61 / Android / iOS 真机与真实移动性能未实测。
